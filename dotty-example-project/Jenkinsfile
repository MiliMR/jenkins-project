pipeline {
    agent any
    environment {
        REPORT_PATH_JENKINS = '**/target/scala-0.24/dependency-check-report.xml'
        REPORT_PATH_ANALYSIS = '../target/scala-0.24/dependency-check-report.xml'
        PROJECT_PATH = 'dotty-example-project'
        MAX_VULNERABILITY = 1
    }
    stages {
        stage('Compile') {
            steps {
                dir("${PROJECT_PATH}"){
                    sh 'sbt clean compile'
                }
            }
        }
        stage('Dependencies Check') {
            steps {
                dir("${PROJECT_PATH}"){
                    sh 'sbt dependencyCheck'
                    archiveArtifacts artifacts: "${REPORT_PATH_JENKINS}", fingerprint: true, onlyIfSuccessful: true
                    dependencyCheckPublisher pattern: "${REPORT_PATH_JENKINS}"
                }
            }
            post{
                unstable{
                    echo 'Could not publish the report'
                }
            }
        }
        stage('Analysis Of Dependency Check Report') {
            steps {
                dir("${PROJECT_PATH}/utils"){
                    sh 'npm install'
                    script{
                        def returnValue = sh(
                            returnStatus: true,
                            script: '''
                                npm test
                            '''
                        )
                        if(returnValue != 0) {
                            currentBuild.result = 'UNSTABLE';
                            currentBuild.description = "Vulnerabilities detected";
                       }
                    }
                }
            }
        }
    }
}
